package numbers;

public class Test {
	public static void main(String[]args) {
		String str="nag1end3ra ba5b7u";

		String[] twoWords=str.split(" ");
		String fisrtWord=twoWords[0];
		String secondWord=twoWords[1];
		System.out.println(fisrtWord);
		System.out.println(secondWord);
		
		int sumOfFirstWord;

/*
		for(int i=0;i<=fisrtWord.length();i++)
		{
		 if(firstWord.charAt(i).isDigit())
		{
		   String num=firstWord.charAT(i);
		     int conversion=num.Interger();
		  sumOfFirstWord=sumOfFirstWord+conversion;
		}
		}
		for(int j=0;j<=secondWord.length();j++)
		{

		 if(secondWord.charAt(j).isDigit())
		{
		  sumOfSecondWord=sumOfFirstWord+str.charAt(j);
		}
		}
		if(sumOfFirstWord==sumSecondWord)
		{
		System.out.println("both values are same");
		}
		else
		{
		System.out.println("both value are not same");
		}*/

}}
